name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: setup pipenv
      run: pip install pipenv
    - run: make up
    - run: make lint TTY=false
    - run: make down
    - run: make sync
    - run: make test
