---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: zip-bionic
    image: ubuntu:18.04
provisioner:
  name: ansible
verifier:
  name: ansible
lint: |
  set -e
  yamllint .
  ansible-lint
