---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: prompt-bionic
    image: ubuntu:18.04
    dockerfile: DockerfileExtended.j2
  - name: prompt-archlinux
    image: archlinux
    dockerfile: DockerfileExtended.j2
provisioner:
  name: ansible
  options:
    user: vagrant
verifier:
  name: ansible
lint: |
  set -e
  yamllint .
  ansible-lint
