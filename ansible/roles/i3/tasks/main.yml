---

- name: install packages
  become: yes
  apt:
    name: i3
    state: present
    update_cache: yes

- name: add config file
  copy:
    src: config
    dest: /home/vagrant/.i3/
    mode: 0664

- name: remove CR code in config file
  replace:
    dest: /home/vagrant/.i3/config
    regexp: '\r'
    replace: ''

- name: add i3status confing file
  copy:
    src: i3status.conf
    dest: /home/vagrant/.i3status.conf
    mode: 0664
